{
  "name": "weddni",
  "private": true,
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "eslint .",
    "format": "prettier --write '**/*.{ts,tsx,md}'"
  },
  "devDependencies": {
    "@weddni/eslint-config": "*",
    "eslint": "^8.57.0",
    "prettier": "^3.3.2",
    "turbo": "^2.0.4"
  }
}